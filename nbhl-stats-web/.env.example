# Supabase project: Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
# Legacy "anon" JWT or new publishable key (sb_publishable_...) — same slot
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Legacy service_role JWT or new secret key (sb_secret_...) — import script only; never Vercel/browser
SUPABASE_SERVICE_ROLE_KEY=
